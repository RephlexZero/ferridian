[package]
name = "ferridian-engine"
description = "Render graph and frame orchestration (no Vulkan initialization lives here)"
authors.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
version.workspace = true

[lints]
workspace = true

[dependencies]
ferridian-contract = { workspace = true }
ferridian-pack-format = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
# The reload integration test proves the generation handshake against the
# real producer (`packc serve`'s write side) instead of a re-implementation.
ferridian-pack-compiler = { workspace = true }
