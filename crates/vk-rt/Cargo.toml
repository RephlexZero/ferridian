[package]
name = "ferridian-vk-rt"
description = "Vulkan device/queue/sync/alloc runtime (ash)"
authors.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
version.workspace = true

[lints]
workspace = true

[dependencies]
ash = { workspace = true }
# The camera uniform block's layout is contract-defined.
ferridian-contract = { workspace = true }
# Pure crate (no Vulkan): the executor consumes the engine's ExecutionPlan.
ferridian-engine = { workspace = true }
gpu-allocator = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
