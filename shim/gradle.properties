# NOTE: best-effort versions, not yet verified against live Maven — building
# the shim requires network resolution of Minecraft itself. `mise run
# shim-build` is deliberately not part of `mise run ci` until these are
# confirmed in the devcontainer (tracked in README follow-ups).
minecraft_version=26.2
loader_version=0.16.14
loom_version=1.10-SNAPSHOT

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
