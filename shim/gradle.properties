# Verified against live Fabric Maven in the devcontainer, 2026-07-14:
# loader 0.19.3 is the current release, Loom 1.17.14 the current stable
# (1.18 is still alpha). minecraft_version must stay in lockstep with the
# contract (crates/contract) and upstream-watch's tracked inventory.
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17.14

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
